openapi: 3.0.3
info:
  title: Example OpenApi description
  version: 0.0.0
components:
  schemas:
    AgencyInvoicingType:
      enum:
        - INVOICE_PER_AGENCY
        - INVOICE_PER_ACCOUNT
      type: string
    AgencyRevenueType:
      enum:
        - RATE_CARD
        - BASED_ON_MEDIA_COST
      type: string
    AgencyServiceType:
      enum:
        - SELF_SERVICE
        - MANAGED_SERVICE
      type: string
    AgencyStatus:
      enum:
        - ACTIVE
        - ARCHIVED
      type: string
    ImageSize:
      enum:
      - 300x250
      - 180x150
      - 125x125
      - OTHER
